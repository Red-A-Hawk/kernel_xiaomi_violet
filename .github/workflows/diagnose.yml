name: Build Kernel (Fixed Toolchain)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      ARCH: arm64
      SUBARCH: arm64

    steps:
    - name: Checkout kernel source
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          git bc bison flex libssl-dev \
          make curl zip unzip \
          python3 openjdk-11-jre \
          gcc-aarch64-linux-gnu \
          gcc-arm-linux-gnueabi

    - name: Download Proton Clang 17
      run: |
        git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

    - name: Setup toolchain
      run: |
        echo "PATH=$GITHUB_WORKSPACE/clang/bin:$PATH" >> $GITHUB_ENV
        echo "CC=clang" >> $GITHUB_ENV
        echo "LLVM=1" >> $GITHUB_ENV
        echo "LLVM_IAS=1" >> $GITHUB_ENV
        echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
        echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-" >> $GITHUB_ENV

    - name: Setup KernelSU Next (SUSFS included)
      run: |
        curl -LSs https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh | bash -

    - name: Load defconfig
      run: |
        make vendor/violet-perf_defconfig

    - name: Build scripts
      run: |
        make scripts

    - name: Enable KernelSU + SUSFS
      run: |
        scripts/config --enable CONFIG_KSU
        scripts/config --enable CONFIG_SUSFS
        scripts/config --enable CONFIG_KSU_SUSFS
        make olddefconfig

    - name: Build kernel
      run: |
        make -j$(nproc)

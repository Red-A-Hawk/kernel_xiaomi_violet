name: Build Kernel Violet (Flashable)

on:
  push:
    branches:
      - sixteen
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout source
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          git bc bison flex libssl-dev \
          make gcc lld \
          libc6-dev libncurses5-dev \
          libncursesw5-dev \
          python3 zip unzip \
          openjdk-17-jdk

    # ===== Proton Clang =====
    - name: Clone Proton Clang
      run: |
        git clone --depth=1 https://github.com/kdrag0n/proton-clang.git proton
        echo "$GITHUB_WORKSPACE/proton/bin" >> $GITHUB_PATH

    # ===== KernelSU Next v1.1.1 =====
    - name: Apply KernelSU Next v1.1.1
      run: |
        git clone https://github.com/KernelSU-Next/KernelSU-Next.git
        cd KernelSU-Next
        git checkout v1.1.1
        cd ..
        bash KernelSU-Next/kernel/setup.sh

    # ===== Defconfig =====
    - name: Load defconfig
      run: |
        make O=out vendor/violet-perf_defconfig

    # ===== Build Kernel =====
    - name: Build kernel
      run: |
        make -j$(nproc) O=out \
          CC=clang \
          LD=ld.lld \
          KCFLAGS=-Wno-error

    # ===== AnyKernel3 =====
    - name: Clone AnyKernel3
      run: |
        git clone -b sixteen https://github.com/Joker-V2/AnyKernel3.git anykernel

    - name: Copy kernel images
      run: |
        cp out/arch/arm64/boot/Image.gz anykernel/Image.gz || true
        cp out/arch/arm64/boot/Image anykernel/Image || true
        cp out/arch/arm64/boot/dtbo.img anykernel/dtbo.img || true

    - name: Pack AnyKernel3 zip
      run: |
        cd anykernel
        zip -r9 ../kernel-violet-KSU-next-unsigned.zip ./*
        cd ..

    # ===== ZipSigner =====
    - name: Download zipsigner
      run: |
        wget -O zipsigner.jar \
        https://github.com/Magisk-Modules-Repo/zipsigner/raw/master/bin/zipsigner-3.0-dexed.jar

    - name: Sign flashable zip
      run: |
        java -jar zipsigner.jar \
          kernel-violet-KSU-next-unsigned.zip \
          kernel-violet-KSU-next.zip

    # ===== Upload =====
    - name: Upload flashable kernel
      uses: actions/upload-artifact@v4
      with:
        name: Violet-Kernel-KSU-Next
        path: kernel-violet-KSU-next.zip

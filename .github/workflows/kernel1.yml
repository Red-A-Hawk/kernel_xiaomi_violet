name: Build Kernel (violet | KernelSU Next v1.1.1)

on:
  push:
    branches:
      - sixteen
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git bc bison flex libssl-dev \
            make ccache curl zip unzip \
            gcc-aarch64-linux-gnu \
            gcc-arm-linux-gnueabi \
            binutils-aarch64-linux-gnu \
            binutils-arm-linux-gnueabi \
            openjdk-11-jre

      - name: Clone Proton Clang
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

      - name: Setup build environment
        run: |
          echo "ARCH=arm64" >> $GITHUB_ENV
          echo "SUBARCH=arm64" >> $GITHUB_ENV
          echo "PATH=$GITHUB_WORKSPACE/clang/bin:$PATH" >> $GITHUB_ENV
          echo "CC=clang" >> $GITHUB_ENV
          echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
          echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-" >> $GITHUB_ENV
          echo "LLVM=1" >> $GITHUB_ENV
          echo "LLVM_IAS=1" >> $GITHUB_ENV

      - name: Apply KernelSU Next v1.1.1
        run: |
          git clone --branch v1.1.1 https://github.com/KernelSU-Next/KernelSU-Next.git ksu
          bash ksu/kernel/setup.sh

      - name: Load defconfig
        run: |
          make O=out vendor/violet-perf_defconfig
          make O=out olddefconfig

      - name: Build kernel
        run: |
          make O=out -j$(nproc)

      - name: Clone AnyKernel3
        run: |
          git clone -b sixteen https://github.com/Joker-V2/AnyKernel3.git anykernel

      - name: Copy kernel images
        run: |
          cp out/arch/arm64/boot/Image.gz anykernel/
          cp out/arch/arm64/boot/dtbo.img anykernel/

      - name: Package AnyKernel3
        run: |
          cd anykernel
          zip -r ../AnyKernel3.zip *

      - name: Download zipsigner
        run: |
          curl -L -o zipsigner.jar \
          https://github.com/Magisk-Modules-Repo/zipsigner/raw/master/bin/zipsigner-3.0-dexed.jar

      - name: Sign ZIP
        run: |
          java -jar zipsigner.jar AnyKernel3.zip violet-KSU-Next-v1.1.1-signed.zip

      - name: Upload signed ZIP
        uses: actions/upload-artifact@v4
        with:
          name: violet-kernel-KSU-Next
          path: violet-KSU-Next-v1.1.1-signed.zip
